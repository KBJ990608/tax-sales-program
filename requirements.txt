# 앱 실행에 필요한 최소 패키지. Streamlit Community Cloud가 이 파일을 읽는다.
# 하한 버전은 실제로 쓰는 API 기준:
#   streamlit  : width="stretch" (구 use_container_width 대체) 는 1.49+
#   anthropic  : messages.parse(output_format=...) 구조화 출력
streamlit>=1.49
pandas>=2.0
anthropic>=0.120
pydantic>=2.0
